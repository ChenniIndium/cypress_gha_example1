version: 2.1

jobs:
  build:
    steps:
      - checkout # Checkout your source code
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run Cypress Tests
          command: npm run cypress:run # Replace with your actual Cypress test command
